# 작은다음 트러블슈팅 기록

서비스를 개발하고 운영하면서 실제로 발생한 문제와 해결 과정을 기록합니다. 단순히 오류 메시지만 남기지 않고, 재현 조건과 원인, 해결 방법, 검증 결과를 함께 적어 같은 문제가 반복되지 않도록 합니다.

## 작성 원칙

- 실제로 확인한 증상과 해결 내용만 기록한다.
- 임시 우회와 근본 해결을 구분한다.
- 사용자 데이터, 비밀 키, 이메일 등 민감한 값은 남기지 않는다.
- 수정 후에는 빌드, 자동 테스트, 브라우저 확인 등 재현 가능한 검증 결과를 기록한다.
- 포트폴리오에서는 기술 선택 자체보다 문제를 좁히고 재발을 막은 과정을 설명한다.

## 1. 여러 프로젝트가 같은 localhost 주소에 표시되는 문제

### 현상

- 다른 프로젝트에서 `npm run dev`를 실행했는데 기존에 실행 중이던 프로젝트가 `localhost:3000`에 표시됐다.
- Next.js가 여러 `package-lock.json`을 감지하고 예상과 다른 상위 폴더를 작업 공간 루트로 선택했다는 경고가 나타났다.

### 원인

- 이미 3000 포트를 사용 중인 개발 서버가 있었다.
- 상위 폴더와 하위 프로젝트에 lockfile이 함께 있어 프레임워크가 작업 공간 루트를 잘못 추론할 여지가 있었다.
- 브라우저 주소는 프로젝트가 아니라 포트를 가리키므로, 같은 포트를 동시에 사용하려 하면 먼저 실행된 서버가 계속 응답한다.

### 해결

- 명령을 실행하기 전에 현재 폴더가 대상 프로젝트 루트인지 확인했다.
- 작은다음의 모바일 개발 서버는 3002 포트를 사용하도록 분리했다.
- 불필요한 중복 lockfile을 정리하고, 필요한 경우 빌드 도구의 루트를 프로젝트 폴더로 명시하도록 구성했다.

### 검증

- `localhost:3002`에서 작은다음 화면이 로드되는지 확인했다.
- 새 브라우저 세션에서도 예상한 제목과 화면이 표시되는지 확인했다.

### 배운 점

포트 충돌은 애플리케이션 오류처럼 보일 수 있지만 실제로는 실행 환경 문제다. 프로세스, 포트, 실행 폴더를 먼저 분리해 확인하면 잘못된 코드를 디버깅하는 시간을 줄일 수 있다.

## 2. Supabase 인증 요청 실패와 중복 회원가입 메시지

### 현상

- 로그인과 회원가입에서 `Failed to fetch`가 표시됐다.
- 이미 가입한 이메일로 다시 회원가입하면 인증 메일이 오지 않았지만, 사용자는 중복 가입인지 알기 어려웠다.

### 원인

- 장기간 사용하지 않은 Supabase 프로젝트가 일시 정지되어 인증 서버에 연결할 수 없었다.
- Supabase Auth는 계정 존재 여부 노출을 줄이기 위해 중복 이메일 가입 시에도 일반 성공 응답과 유사한 결과를 줄 수 있다.

### 해결

- Supabase 프로젝트를 재개하고 환경 변수의 프로젝트 URL과 공개 키를 다시 확인했다.
- 네트워크 연결 실패, 비밀번호 불일치, 이름 검증 실패, 중복 이메일 가능성을 서로 다른 안내 문구로 분기했다.
- 중복 이메일로 판단되는 경우 가입을 반복하지 않고 로그인 또는 비밀번호 찾기로 안내했다.

### 검증

- 기존 계정 로그인과 신규 계정 회원가입 흐름을 각각 확인했다.
- 비밀번호와 비밀번호 확인 값이 다를 때 서버 요청 전에 안내가 표시되는지 확인했다.

### 배운 점

인증 오류는 서버 응답만 그대로 보여주기보다 네트워크, 입력 검증, 보안 정책을 구분해 사용자 행동으로 이어지는 메시지로 바꿔야 한다. 다만 계정 존재 여부를 명시하는 정책은 공개 서비스에서 이메일 열거 공격 위험과 함께 다시 검토해야 한다.

## 3. Supabase Row Level Security로 데이터 저장이 거부되는 문제

### 현상

수입 또는 지출을 저장할 때 `new row violates row-level security policy` 오류가 발생했다.

### 원인

- 테이블의 RLS는 활성화되어 있었지만 인증 사용자에게 필요한 `INSERT` 정책이 없거나, 저장하려는 `user_id`가 `auth.uid()`와 일치하지 않았다.
- 사용자별 데이터 분리를 위해 둔 보안 규칙과 애플리케이션의 저장 값이 맞지 않았다.

### 해결

- 사용자 데이터 테이블마다 인증된 사용자 역할을 대상으로 조회·등록·수정·삭제 정책을 명시했다.
- 등록과 수정 정책에서 `auth.uid() = user_id` 조건을 적용했다.
- 클라이언트가 임의 사용자 ID를 사용하지 않고 현재 인증 세션의 ID를 저장하도록 통일했다.

### 검증

- 일정, 거래, 감정, 하루 기록을 로그인 사용자 계정으로 생성하고 다시 조회했다.
- 다른 사용자 ID의 행에는 접근할 수 없도록 정책 조건을 확인했다.

### 배운 점

RLS 오류는 단순 권한 해제로 해결하면 안 된다. 프런트엔드에서 전달한 소유자 값과 데이터베이스 정책을 함께 추적해야 사용자 데이터 격리를 유지하면서 정상 저장을 복구할 수 있다.

## 4. `jwt issued at future`로 데이터 로딩이 간헐적으로 실패하는 문제

### 현상

가끔 데이터 화면 대신 `jwt issued at future`와 다시 시도 안내가 표시됐다.

### 원인

- 기기 시간과 인증 서버 시간 사이에 차이가 있으면 JWT의 발급 시각이 서버 기준 미래로 판단될 수 있다.
- 인증 직후 토큰 갱신과 데이터 요청이 겹치는 짧은 구간에서도 일시적인 검증 실패가 발생할 수 있었다.

### 해결

- 일시적 인증 오류는 0.8초, 1.8초, 3.5초, 6.5초 간격으로 제한적으로 재시도하도록 했다.
- 재시도 후에도 실패할 때만 사용자가 이해할 수 있는 복구 안내를 표시했다.
- 같은 문제가 반복되면 기기의 날짜와 시간 자동 설정을 확인할 수 있도록 원인을 구분했다.

### 검증

- 최초 데이터 로딩과 세션 복원 경로에서 재시도 후 정상 화면으로 복귀하는지 확인했다.
- 영구적인 오류가 무한 재시도로 가려지지 않는지 확인했다.

### 배운 점

네트워크와 인증은 순간적으로 실패할 수 있다. 모든 오류를 즉시 치명적인 상태로 처리하기보다 재시도 가능한 오류만 식별하고, 횟수와 간격을 제한하는 복원 전략이 필요하다.

## 5. Vinext 개발 서버에서 Nitro 플러그인이 충돌하는 문제

### 현상

개발 서버 실행 중 `Cannot read properties of undefined (reading 'import')` 오류가 발생해 페이지가 열리지 않았다.

### 원인

- 배포용 서버 번들을 만드는 Nitro 플러그인이 개발 모드에서도 함께 로드되면서 Vinext의 개발 실행 환경과 충돌했다.
- 개발 서버와 운영 빌드에 필요한 플러그인 구성이 같다고 가정한 것이 문제였다.

### 해결

- `vite.config.ts`에서 Nitro 플러그인을 운영 빌드일 때만 포함하도록 조건을 분리했다.
- 개발 모드에서는 Vinext 개발 플러그인만 사용하고, Vercel 빌드에서는 Nitro 출력물을 생성하도록 구성했다.

### 검증

- 개발 서버가 정상 응답하는지 확인했다.
- 별도로 운영 빌드를 실행해 Nitro 기반 배포 산출물이 생성되는지 확인했다.

### 배운 점

빌드 도구 문제는 패키지 버전만 바꾸기 전에 개발과 운영 실행 경로를 분리해 봐야 한다. 환경별 책임을 명확히 나누면 로컬 개발 편의와 배포 호환성을 함께 유지할 수 있다.

## 6. 모바일에서 화면이 오른쪽으로 밀리고 확대되어 보이는 문제

### 현상

- 모바일 접속 시 전체 화면이 오른쪽으로 치우치고, 처음부터 확대된 것처럼 일부 내용만 보였다.
- PC용 사이드 내비게이션 영역이 모바일 본문 폭에도 영향을 주었다.

### 원인

- 고정 폭 사이드바와 본문 여백이 좁은 화면에서도 유지됐다.
- 일부 자식 요소의 최소 너비가 화면보다 커져 가로 오버플로가 발생했다.
- PC와 모바일의 탐색 구조를 크기만 줄여 공유하면서 터치 환경의 공간 제약을 충분히 반영하지 못했다.

### 해결

- 모바일에서는 PC 사이드바를 숨기고 모바일 전용 탐색을 사용했다.
- 본문과 카드에 `min-width: 0`, 반응형 여백, 가로 오버플로 방지 규칙을 적용했다.
- 설치 안내와 주요 동작을 작은 화면의 읽기 순서에 맞게 재배치했다.

### 검증

- 모바일 크기에서 별도 축소 없이 전체 폭이 맞는지 확인했다.
- 하단 탐색과 주요 입력 화면을 터치로 사용할 수 있는지 확인했다.

### 배운 점

반응형 디자인은 단순한 비율 축소가 아니다. 정보 우선순위와 탐색 방식을 기기별로 다시 설계해야 하며, 가로 오버플로는 부모뿐 아니라 자식 요소의 최소 너비까지 추적해야 한다.

## 7. PWA 브랜드 변경 후 이전 캐시가 남는 문제

### 현상

서비스 이름과 아이콘을 바꾼 뒤에도 설치된 앱에서 이전 브랜드의 리소스가 남을 가능성이 있었다.

### 원인

- 서비스 워커는 정적 리소스를 캐시하므로 새 배포가 완료되어도 기존 캐시 이름을 정리하지 않으면 오래된 화면이나 아이콘을 계속 사용할 수 있다.

### 해결

- 새 캐시 이름을 `jakda-shell-v1`로 변경했다.
- 서비스 워커 활성화 단계에서 현재 `jakda-` 캐시를 제외한 새 브랜드의 오래된 캐시와 기존 `ongyeol-` 캐시를 함께 정리하도록 했다.
- manifest, 앱 아이콘, 파비콘, Open Graph 이미지를 같은 브랜드 체계로 맞췄다.

### 검증

- PWA 관련 정적 파일이 빌드 결과에 포함되는지 확인했다.
- 새 브라우저 세션에서 작은다음 브랜드와 새 아이콘이 표시되는지 확인했다.

### 배운 점

PWA의 브랜드 변경은 화면 문자열 교체로 끝나지 않는다. 캐시 수명 주기와 설치 메타데이터까지 마이그레이션해야 기존 설치 사용자도 안전하게 새 버전으로 이동한다.

## 8. CSS 구조 변경 중 개발 서버가 일시적으로 500을 반환한 문제

### 현상

큰 CSS 파일을 여러 파일로 나누는 중 개발 서버가 아직 생성되지 않은 파일을 import해 일시적으로 500 오류를 반환했다.

### 원인

- 파일 이동과 import 변경 사이의 짧은 시점에 HMR이 중간 상태를 감지했다.
- 구조 변경 전의 모듈 그래프가 개발 서버 메모리에 남아 있었다.

### 해결

- 스타일 파일을 역할별로 `foundation`, `brand`, `readability`, `responsive`, `application`으로 분리했다.
- 모든 파일과 import가 완성된 뒤 개발 서버를 다시 시작해 모듈 그래프를 새로 구성했다.

### 검증

- 새 브라우저 세션에서 콘솔 오류 없이 메인 화면이 로드되는지 확인했다.
- 린트, 운영 빌드, 자동 테스트, 포맷 검사를 모두 통과시켰다.

### 배운 점

HMR은 빠른 피드백에는 유리하지만 대규모 파일 이동 중간 상태까지 실행할 수 있다. 구조 변경 후에는 새 프로세스와 새 브라우저 세션으로 검증해야 캐시된 성공 상태나 오류에 속지 않는다.

## 9. Vercel 프로젝트 이름·도메인과 실제 배포 버전이 어긋나는 문제

### 현상

- Vercel 프로젝트와 도메인을 `jakda`로 바꿨지만 운영 주소에는 이전 배포의 온결 화면이 계속 표시됐다.

### 원인

- 프로젝트 이름과 도메인 연결은 배포할 주소를 정할 뿐, 로컬 변경사항을 자동으로 새 배포에 포함하지 않는다.
- 운영 배포 브랜치와 기능 개발 브랜치가 분리되어 있지 않아 어떤 커밋이 운영에 반영되는지 명확하지 않았다.

### 해결

- 운영 전용 `release` 브랜치를 만들고 Vercel의 Production Branch를 `release`로 지정했다.
- 기능과 오류 수정은 별도 브랜치에서 검증한 뒤에만 `release`에 반영한다.
- `master`에 푸시해도 운영 배포가 발생하지 않도록 역할을 분리한다.

### 검증

- Vercel 설정을 다시 열어 Production Branch가 `release`로 유지되는 것을 확인했다.
- `release`의 초기 커밋 `168dca9`로 생성된 Preview 배포가 Ready 상태인 것을 확인했다.
- 설정 완료 후 새 문서 커밋을 푸시해 자동 Production 배포가 시작되는지 확인한다.
- `https://jakda.vercel.app`에서 작은다음 브랜드와 최신 기능이 표시되는지 확인한다.

### 배운 점

도메인, 프로젝트 이름, 소스 브랜치, 배포 결과는 서로 다른 설정이다. 운영 브랜치를 명시하면 배포 기준을 문서화할 수 있고, 개발 중간 상태가 운영으로 나가는 위험도 줄어든다.

## 10. Vercel에 환경 변수를 추가했지만 운영 API에서 인식하지 못하는 문제

### 현상

- Vercel에 `KAKAO_REST_API_KEY`를 등록한 뒤에도 운영 장소 검색 API가 503 상태와 함께 “장소 검색 API 키가 설정되지 않았어요.”를 반환했다.

### 원인

- Vercel의 환경 변수는 배포를 만들 때 해당 배포에 주입된다.
- 이미 실행 중인 배포는 설정 이후에 추가된 환경 변수를 자동으로 가져오지 않는다.

### 해결

- 환경 변수의 이름과 적용 환경에 Production이 포함됐는지 확인했다.
- `release` 브랜치에 새 커밋을 푸시해 Production 배포를 다시 만들도록 했다.

### 검증

- 새 운영 배포가 Ready 상태가 된 뒤 `/api/places?q=서울숲`을 호출했다.
- 키가 없는 기존 배포의 503 응답은 사라졌지만 Kakao가 403을 반환해, 환경 변수 주입 이후의 Kakao 앱 권한 경계에서 요청이 거부되는 것을 확인했다.
- REST API 키의 로컬 API 사용 권한과 호출 허용 IP 설정을 확인한 뒤 200 응답과 `places` 배열에 서울숲 검색 결과가 포함되는지 다시 확인한다.

### 배운 점

환경 변수 설정 화면의 값과 실행 중인 배포의 환경은 동일하지 않을 수 있다. 외부 API 연동을 점검할 때는 변수 존재 여부뿐 아니라 그 변수가 포함된 시점의 배포인지 함께 확인해야 한다.

## 11. 네이버 개발자센터에서 지역 검색 API가 보이지 않는 문제

### 현상

- 네이버 개발자센터에서 새 Application을 등록할 때 로그인, 카페, 캘린더 등의 API만 보이고 검색 또는 지역 API를 선택할 수 없었다.

### 원인

- 네이버는 2026년 7월 31일부터 검색 API의 신규 신청 창구를 기존 개발자센터에서 NAVER Cloud Platform의 NAVER API HUB로 이전했다.
- 이전 개발자센터의 등록 화면을 기준으로 설정을 시도하면 현재는 지역 검색 권한을 발급받을 수 없다.

### 해결

- NAVER Cloud Platform에서 NAVER API HUB 이용을 신청했다.
- NAVER 검색 카테고리의 `지역` API로 Application을 등록했다.
- 서버 라우트를 NAVER API HUB의 `/search/v1/local` 엔드포인트와 새 인증 헤더 방식으로 변경했다.
- Client ID와 Client Secret을 각각 `NAVER_API_HUB_CLIENT_ID`, `NAVER_API_HUB_CLIENT_SECRET` 서버 환경 변수로 분리했다.

### 검증

- 로컬과 Vercel 환경에 두 인증 값을 등록한 뒤 `/api/places?q=서울숲`이 장소 이름과 주소 배열을 반환하는지 확인한다.
- 일정 작성 화면에서 검색 결과를 선택했을 때 장소명과 주소가 함께 입력되는지 확인한다.

### 배운 점

외부 API 문서는 검색 결과에 이전 절차가 함께 노출될 수 있다. 등록 화면이 문서와 다르면 사용자의 조작을 의심하기 전에 서비스 이전 공지, 신규 신청 중단 여부, 인증 체계 변경을 먼저 확인해야 한다.
